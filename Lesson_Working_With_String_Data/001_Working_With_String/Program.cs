namespace _001_Working_With_String
{
    //In C#, string is a reference type that represents a sequence of characters.
    //It's part of the .NET Framework's System namespace, so you don't need to import anything special to use it.
    internal class Program
    {
        static void Main(string[] args)
        {
            //Creating Strings:
            string str1 = "Hello";

            string str2 = new string('c', 3); // Creates "ccc"
        }
    }
}
